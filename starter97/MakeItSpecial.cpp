#include <iostream>
#include <bits/stdc++.h>
#define ll long long int
using namespace std;
int main(){
    ll t;
    cin >> t;
    while(t--)
    {
        ll n , q;
        cin >> n >> q;
        ll l[q],r[q];
        for(ll i=0;i<1;i++)
        {
            cin >> l[i] >> r[i];
        }
        // l to l+n-1 exactly once 
        // l smallest element
        
    }
    return 0;
}