#include <iostream>
#define ll long long int
using namespace std;
int main(){
    ll t;
    cin >> t;
    while(t--){
        ll n;
        cin >> n;
        string s[n];
        for(ll i=0;i<n;i++)
        {
            cin >> s[n];
        }
        // max weapons winner has at last
        // 10 weapons
        // jth char of Si--1
        // ith contestant has weapon j or '0'
        // each j if same weapon type j both are destroyed
        
    }
    return 0;
}